# Design QA：地图、Agent 与状态区边缘拖拽

## 验收基准

- 交互参考：`/var/folders/72/9l9vk1dn40vd7sj8v3s8l7qh0000gn/T/codex-clipboard-c42e94c7-2940-480e-af46-33cf60cef36f.png`
- 实现截图：`/Users/chl/Codespace/TF-agent/docs/dev/ui-audit/resize-controls-after.jpg`
- 并排对比：`/Users/chl/Codespace/TF-agent/docs/dev/ui-audit/resize-controls-comparison.jpg`
- 验收视口：1280 × 720 CSS px；实现截图为 1280 × 720 px。
- 参考图为现场交互示意，用于校验“边缘命中并拖拽”的操作逻辑，不作为像素级视觉复刻目标。

## 本轮对比记录

### 第一次复现

- P1：按下地图/Agent 竖向分界线后，指针离开窄边缘或跨入地图 iframe，后续移动与抬起事件丢失，宽度不变化且拖拽状态残留。
- P1：状态/日志水平分界线存在同类问题，高度无法持续调整。
- P1：Agent 宽度变化时，状态/日志区边界没有可靠地跟随地图宽度同步。

### 修复后复核

- 使用父页面内联拖拽处理器，并在拖动期间建立透明捕获层；指针跨过地图 iframe 后仍能持续接收移动和结束事件。
- Agent 分界线拖动后，地图宽度约 529 px、Agent 宽度约 419 px；状态/日志水平边界同步缩至约 525 px。
- 状态边界上移后，状态预留高度由 228 px 调整到 326 px；地图高度同步收缩，拖拽结束后捕获层和 `body` 拖拽状态均已清理。
- 折叠并再次展开状态区后，两个分隔条仍为当前版本并可继续拖动，说明 Streamlit rerun 后已完成重绑定。
- 两个边缘命中层已提高层级，地图内容不会再截获分界线中央区域的拖拽。

## 视觉与交互检查

- 全视图：参考图与实现均通过地图/侧栏竖向边界、地图/状态区横向边界完成尺寸调整；实现保留现有三栏产品布局。
- 聚焦检查：并排对比图同时覆盖两个分隔条、状态区展开状态和 Agent 输入区，不需要额外裁切才能判断交互位置。
- 键盘与辅助信息：分隔条保留 `role="separator"`、方向、当前值和允许范围；方向键仍可调整。
- 浏览器控制台：未发现本项目拖拽脚本异常。内置浏览器仍会记录一条 `browser-page-preload.js` 的 MutationObserver 错误，已通过脚本来源定位确认为 Codex 浏览器预加载代码，不属于本仓库实现。

## 最终结论

- P0：无。
- P1：无。
- P2：无。
- 结果：**通过**。边缘横向/纵向拖拽、跨 iframe 捕获、联动尺寸同步以及 rerun 后重绑定均已完成真实浏览器验证。
